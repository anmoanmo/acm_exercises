//
// Created by ᕕ(◠ڼ◠)ᕗ on 24-8-5.
//


#include<bits/stdc++.h>

#define ll long long
using namespace std;

void sol() {
    int n, k;
    cin >> n >> k;
    vector<ll> a(n);
    for (int i = 0; i < n; i++) {
        cin >> a[i];
    }

}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int t = 0;
    cin >> t;
    while (t--) {
        sol();
    }
}