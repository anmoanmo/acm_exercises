//
// Created by ᕕ(◠ڼ◠)ᕗ on 24-7-24.
//


#include<bits/stdc++.h>

#define ll long long
using namespace std;

void sol() {
    int a, b, c, m;
    cin >> a >> b >> c >> m;
    vector<vector<vector<int>>> d(a, vector<vector<int>>(b, vector<int>(c, 0)));
    vector<vector<vector<int>>> s(a, vector<vector<int>>(b, vector<int>(c, 0)));
    for(int i=0;i<a*b*c;i++){

    }

    while (m--) {
        int al, ar, bl, br, cl, cr, h;
        cin >> al >> ar >> bl >> br >> cl >> cr >> h;

    }
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    sol();
}